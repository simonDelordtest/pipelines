apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: create-git-repos-task
spec:
  steps:
    - name: echo
      image: fedora
      script: |
        #!/usr/bin/env bash
        curl -u 'simonDelordtest:08cf2c22f44006e680e1d868fdc3daba8c4247b9' https://api.github.com/user/repos -d '{"name":"application1"}'

