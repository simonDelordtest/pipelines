apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: create-a-dev-git-repo
spec:
  steps:
    - name: echo
      image: fedora
      script: |
        #!/usr/bin/env bash
        curl -u 'simonDelordtest:f6c80580cdb018d5d004f45265ab7277e66caecf' https://api.github.com/user/repos -d '{"name":"dev-repo-app1"}'

