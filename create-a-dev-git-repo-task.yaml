apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: create-a-dev-git-repo
spec:
  steps:
    - name: echo
      image: fedora
      script: |
        #!/usr/bin/env bash
        curl -u 'simonDelordtest:33b691fd294a929c4c316b559862ebcb43cd6003' https://api.github.com/user/repos -d '{"name":"dev-repo-app1"}'

